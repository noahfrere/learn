package jayJay;

public class SqrtFunction {
	
	public SqrtFunction(){
		
	}

	public void get(double number){
	
		double x = 0;
		number = x;
		double x1, x2, x3;
		double S = 0;
		
		x1 = x - (((x*x)-S) / (2*x));
		x2 = x1 - (((x1*x1)-S) / (2*x1));
		x3 = x2 - (((x2*x2)-S) / (2*x2));
		return;
	}
	
}
	
